import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  signOutButton: {
    marginTop: 30,
    alignItems: 'center',
  },
  signOutText: {
    color: 'red',
    fontSize: 16,
  },
});
